# LeetCode Credentials (used if cookies are not provided)
LEETCODE_USERNAME=
LEETCODE_PASSWORD=

# Cookie-based auth (preferred)
# Provide either a full cookie header string or specific cookies
# Example LEETCODE_COOKIES: "LEETCODE_SESSION=...; csrftoken=..."
LEETCODE_COOKIES=
LEETCODE_SESSION=
LEETCODE_CSRFTOKEN=

# Optional: Git push target
# GIT_REMOTE=origin
# GIT_BRANCH=main

# Optional: poll mode (seconds). If set > 0, the script will run in a loop.
# SYNC_POLL_SECONDS=300
